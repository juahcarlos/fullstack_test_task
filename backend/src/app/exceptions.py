"""Собственные исключения приложения, независимые от FastAPI.

Сервисный слой (services.py) не должен знать про HTTP — он бросает эти
исключения, а перевод в конкретные HTTP-статусы происходит в одном месте:
exception handler'ах в main.py.
"""


class FileNotFoundInStorageError(Exception):
    """Файл не найден: либо нет записи в БД, либо нет физического файла на диске."""


class EmptyFileError(Exception):
    """Загруженный файл оказался пустым."""